substitutions:
  _IMAGE: ""

steps:
  - name: gcr.io/cloud-builders/docker
    args:
      - build
      - -f
      - apps/medusa/Dockerfile
      - -t
      - ${_IMAGE}
      - .
images:
  - ${_IMAGE}
timeout: 1200s
